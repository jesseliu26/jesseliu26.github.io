# 博客功能备忘录

最后整理：2026-05-17

这个文件用来记录博客已经完成了什么、还想做什么、以及临时冒出来的想法。它不是发布文章，想到什么可以直接往下面补。

## 已完成功能

- [x] 静态个人博客基础页面，可以部署到 GitHub Pages。
- [x] 顶部导航已移除，页面保留左侧作者信息面板并直接进入文章列表。
- [x] Hash 路由：`#/`、`#/tag/:tag`、`#/post/:slug`。
- [x] Markdown 文章系统：通过 `posts/index.json` 管理文章列表，并读取 `posts/*.md`。
- [x] 文章 front matter：支持 `title`、`date`、`readTime`、`tags`、`theme`、`excerpt`。
- [x] 首页文章卡片：标题、日期、阅读时间、摘要、栏目和自动生成封面。
- [x] 文章搜索：可按标题、摘要、栏目和正文内容搜索。
- [x] 标签筛选：根据文章 front matter 自动生成筛选项。
- [x] 文章详情页：包含返回列表、文章头图、正文和右侧目录。
- [x] Markdown 基础渲染：标题、段落、链接、加粗、行内代码、引用、列表和代码块。
- [x] 文章目录：根据二级、三级标题自动生成。
- [x] 亮色/暗色主题切换，并使用 `localStorage` 记住选择。
- [x] Canvas 自动生成文章封面和个人头像兜底图。
- [x] 首页卡片区块已移除，当前首页直接进入全部文章。
- [x] 左侧个人介绍拆到 `profile/index.json`。
- [x] 新增 `assets/images/avatar/` 个人头像目录和 `assets/images/covers/` 文章封面目录。
- [x] 新增 `assets/images/posts/` 正文配图目录，并支持独立图片段落图注。
- [x] 完成仓库安全检查，并添加基础 CSP、`SECURITY.md` 和 Dependabot 配置。
- [x] 响应式布局：适配桌面、平板和手机宽度。
- [x] 文章加载失败时显示提示。
- [x] 404 页面。
- [x] 已准备 CNAME，用于自定义域名。

## 未实现 / 待完善功能

- 暂无。

## 手动更新约定

- 已经做完的功能放到“已完成功能”，用 `- [x]`。
- 明确要做但还没做的放到“未实现 / 待完善功能”，用 `- [ ]`。
- 只是灵感、暂时不确定做不做的放到“想法池”。
- 完成某项功能后，把对应条目从 `- [ ]` 改成 `- [x]`，也可以顺手挪到“已完成功能”。
